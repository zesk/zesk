-- Database: mysql
CREATE TABLE {table} (
	ID					integer unsigned PRIMARY KEY AUTO_INCREMENT NOT NULL,
	Contact				integer unsigned NOT NULL,
	Whois_Contact_Type	integer unsigned NOT NULL,
	Whois_Result		integer unsigned NOT NULL,
	KEY Contact (Contact),
	KEY Contact (Contact),
	KEY Contact (Contact)
);
